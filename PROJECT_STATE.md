# PROJECT_STATE

更新时间：2026-07-20

## 已完成并验证

- V1 已按新项目方式重写，为无框架、无运行依赖的静态 PWA。
- 本地数据层使用 IndexedDB，并提供内存实现用于测试。
- 已实现多宝宝、每日动态作息、当前/下一事项、日常与睡眠记录、成长时间线和曲线。
- 已实现 600 道食谱、7 天菜单、安全过滤、替换、收藏、偏好、新食物观察和购物清单。
- 已实现 JSON 备份导出、校验导入、全量清除、Service Worker 离线缓存和通知权限状态。
- 自动化测试命令为 `npm.cmd test`；2026-07-20 最近一次运行结果为 46 项全部通过。
- 已完成浏览器手工验收：首次建档、当前事项推进、生成 14 餐菜单、快速记录与时间线展示。

## 明确边界

- 不包含照片功能、旧版本数据迁移、账号、云同步、支付或商业化能力。
- 后台通知为浏览器能力范围内的尽力提醒，不是可靠的系统闹钟。
- 数据只保存在当前浏览器；跨设备需要手动导出/导入备份。

## 发布状态

- 开发分支：`feature/v1-rewrite`
- 已修复跨日任务污染，并加强备份语义校验、餐次状态和购物清单重复生成处理。
- 已补齐作息事项手动调整/保留后续时间、记录编辑删除与睡眠流程、新食物反应、提醒管理和通知调度、时间线事件及 PWA 更新确认。
- 第二轮审查提出的严格备份校验、睡眠管理、提醒调度和独立缓存版本问题已修复；最终复核无 Critical 或 Important。
- 合并与 GitHub 推送状态：已合并到 `main`，并于 2026-07-20 推送到 GitHub。

## 2026-07-23 移动端与睡眠联动优化

- 夜间睡眠结束会作为当天起床锚点：例如 07:00 起床后，早间喝奶调整为 07:20，并按作息规则顺延后续未锁定事项。
- 午间小睡结束后，下一项可用辅食按模板中的间隔调整；默认间隔为 120 分钟。
- 新增睡眠记录、补记、编辑、删除及跨日期调整都会原子恢复并重放受影响日期的作息锚点。
- 今日事项主操作精简为“完成”和“更多操作”；其他处理方式收纳到可访问的模态操作框。
- 睡眠计时区每个状态只展示一个主操作，补记睡眠作为次级入口。
- 菜单与食谱只在展示层隐藏“·搭配数字”，并补充食材用量、主食、质地、阶段和独立状态标签。
- Service Worker 缓存版本升级为 `baby-growth-v1-r4`。
- 自动化验证命令为 `npm.cmd test`；本阶段最新结果为 82 项全部通过，`npm.cmd run check` 与 `git diff --check` 通过。

## 2026-08-12 V2 食谱浏览

- 食谱库支持阶段、关键词、咀嚼入门/进阶和手指食物组合筛选。
- 咀嚼食谱可用原生 `details` 展开配料克数、步骤、入口尺寸、软硬度测试、过敏原、替换建议、餐次和保存信息。
- 旧食谱缺少扩展字段时显示“待补充”，不推断安全、过敏原或进食方式信息。
- 320px 宽度下筛选控件与详情区域改为单列，卡片长文本不会造成横向溢出。
- Service Worker 缓存版本升级为 `baby-growth-v1-20260720-r10`。
- 食谱库初始仅渲染当前筛选结果的前 24 道，并可逐批加载更多；筛选状态在应用刷新后保留。
- 收藏和“不喜欢”通过目录查表保留数字或字符串食谱 ID 的真实类型，V2 字符串 ID 可正常持久化。
- Service Worker 缓存版本升级为 `baby-growth-v1-20260720-r11`。

## 2026-08-11 V2 恢复优先引导

- 空数据库首次启动不会预写 `appSettings.dataVersion`，会先提供 V1 JSON 备份恢复入口，再提供新建宝宝表单。
- 已有宝宝的数据在启动时通过 V2 审计并写入 `dataVersion: 2`，同时保留有效的当前宝宝选择并跳过首次引导。
- V1 备份可在首次引导中原子恢复；成功后进入原宝宝首页并显示恢复宝宝数与记录数，失败会显示错误、清空文件输入且不部分写入。
- 若数据已完整恢复但首页启动失败，引导层会保留并显示持久状态，全部表单控件会锁定，程序化提交也会被阻止，避免混入新宝宝或重复导入。
- 新建宝宝流程继续可用，并与恢复流程共享主应用启动逻辑。
- Service Worker 缓存版本升级为 `baby-growth-v1-20260720-r7`。
- 自动化验证结果为 101 项全部通过，`npm.cmd run check` 与 `git diff --check` 通过。

## 2026-08-12 V2 便便与尿尿快捷记录

- 今日页和记录页均提供独立的“记录便便”“记录尿尿”一键入口，写入当前宝宝的 `dailyRecords`，每次默认记录 1 次。
- 新记录仅使用 `stool` 与 `urine` 两种类型；旧 `toilet`/`stool` 数据不改写，尿尿不会混入旧排便数据。
- 快捷写入期间按钮会锁定，失败时显示可见错误；今日页原有喝奶、喝水与睡眠入口继续保留。
- 320px 手机布局下五个快捷入口按三列换行，保持统一触控尺寸。
- 便便和尿尿记录可编辑发生时间、正整数次数及备注；保存失败时编辑框保持打开并显示错误。
- 编辑保存期间会锁定提交、取消和背景关闭路径，避免看似取消但后台仍写入；失败后恢复取消能力。
- Service Worker 缓存版本升级为 `baby-growth-v1-20260720-r15`。
- 生活趋势已接入当前宝宝的本地记录：默认显示 7 天睡眠，可切换奶量、便便、尿尿以及 7/14/30 天；记录或睡眠变更后刷新，切换宝宝和导入备份时重置选择。
- 日常记录或睡眠存储区读取失败时，五个主页面仍会渲染；记录页与成长趋势会明确显示数据暂不可用，不把空列表冒充真实的零记录。
- 趋势选择在切换、删除宝宝及导入备份后重置；Service Worker 缓存版本升级为 `baby-growth-v1-20260720-r18`。
- 自动化验证结果为 201 项全部通过，`npm.cmd run check` 与 `git diff --check` 通过。
