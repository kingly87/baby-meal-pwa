const definitions={growthMeasurements:{type:'growth',date:item=>item.date,title:item=>`成长记录：${item.weight?`体重 ${item.weight} kg`:''}${item.height?` 身高 ${item.height} cm`:''}`.trim()},toothRecords:{type:'tooth',date:item=>item.date,title:item=>`记录第 ${item.number} 颗牙`},dailyRecords:{type:item=>item.type,date:item=>item.occurredAt||item.date,title:item=>item.type==='milk'?`喝奶 ${item.value} ml`:item.type==='water'?`喝水 ${item.value} ml`:'日常记录'},sleepSessions:{type:'sleep',date:item=>item.endAt||item.startAt,title:item=>item.endAt?`睡眠 ${item.durationMinutes} 分钟`:'开始睡眠'},taskInstances:{type:'task',include:item=>item.status==='completed',date:item=>item.actualAt,title:item=>`完成：${item.title}`},newFoodObservations:{type:'new-food',date:item=>item.date,title:item=>`第一次尝试 ${item.name}`},reminders:{type:'reminder',date:item=>item.completedAt||item.dueDate,title:item=>item.title}};
export function buildTimeline(data,{babyId,type}={}){const result=[];for(const[store,def]of Object.entries(definitions))for(const item of data[store]||[]){const itemType=typeof def.type==='function'?def.type(item):def.type;if(item.babyId!==babyId||def.include&&!def.include(item)||type&&itemType!==type)continue;result.push({id:`${store}:${item.id}`,sourceId:item.id,type:itemType,date:def.date(item),title:def.title(item)})}return result.filter(item=>item.date).sort((a,b)=>b.date.localeCompare(a.date))}
