export function chartModel(records,field){const valid=records.filter(item=>item.date&&item[field]!==''&&Number.isFinite(Number(item[field]))).sort((a,b)=>a.date.localeCompare(b.date));if(valid.length<2)return{points:[],ready:false};const values=valid.map(item=>Number(item[field])),min=Math.min(...values),max=Math.max(...values),range=max-min||1;return{ready:true,min,max,points:valid.map((item,index)=>({date:item.date,value:Number(item[field]),x:index/(valid.length-1),y:1-(Number(item[field])-min)/range}))}}
